{
  "name": "{{REPO}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "{{DESC}}",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "gate": "node --test"
  }
}
